import Styles from './ChangeClassic.module.css'

const ChangeColorClassic = (props) => {
    const ChangeProperties = () => {

        //Insert the fucking code here :)

    }

    return <button onClick={ChangeProperties} className={Styles.botonClassic}>{props.name}</button>
}

export default ChangeColorClassic