import Styles from './ChangeImage.module.css'

const ChangeColorImage = (props) => {
    const ChangeProperties = () => {

        //Insert the fucking code here :)

    }

    return <button onClick={ChangeProperties} className={Styles.botonImage}>{props.name}</button>
}

export default ChangeColorImage